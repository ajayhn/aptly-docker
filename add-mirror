# Adds all Ubuntu mirrors for release $1 

docker run --net=bridge -v /media/ubuntu-mirror:/data/aptly narkisr/aptly aptly -architectures='amd64,i386' mirror create -with-sources=false -ignore-signatures=true ubuntu http://il.archive.ubuntu.com/ubuntu/ $1 main restricted universe multiverse
